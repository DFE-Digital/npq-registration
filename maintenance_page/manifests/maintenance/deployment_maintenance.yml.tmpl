apiVersion: apps/v1
kind: Deployment
metadata:
  name: teacher-training-entitlement-maintenance
  labels:
    app: teacher-training-entitlement-maintenance
spec:
  replicas: 2
  selector:
    matchLabels:
      app: teacher-training-entitlement-maintenance
  template:
    metadata:
      labels:
        app: teacher-training-entitlement-maintenance
    spec:
      nodeSelector:
        teacherservices.cloud/node_pool: applications
        kubernetes.io/os: linux
      containers:
      - name: teacher-training-entitlement-maintenance
        image: ghcr.io/dfe-digital/teacher-training-entitlement-maintenance:#MAINTENANCE_IMAGE_TAG#
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: 64M
            cpu: 0.1
          limits:
            memory: 64M
            cpu: 1
