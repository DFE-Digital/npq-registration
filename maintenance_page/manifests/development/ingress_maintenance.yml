apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: teacher-training-entitlement-maintenance.test.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
  - host: teacher-training-entitlement-maintenance.test.teacherservices.cloud
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: teacher-training-entitlement-maintenance
            port:
              number: 80
