<% if @changes&.any? %>
  <div class="moj-timeline">
    <% @changes.each do |change| %>
      <div class="moj-timeline__item">
        <div class="moj-timeline__header">
          <h2 class="moj-timeline__title"><%= change[:title] %></h2>
          <p class="moj-timeline__byline">by <%= change[:by] %></p>
        </div>
        <p class="moj-timeline__date">
          <time datetime="<%= change[:at].rfc3339 %>"><%= change[:at].to_fs(:govuk_short) %></time>
        </p>
        <% if change[:description].present? %>
          <% if change[:description][:inset] %>
            <%= helpers.govuk_inset_text(text: change[:description][:inset]) %>
          <% end %>
          <% if change[:description][:details] %>
            <%= helpers.govuk_details(summary_text: change[:description][:details_summary], text: change[:description][:details]) %>
          <% end %>
          <% if change[:description][:bullet] %>
            <%= helpers.govuk_list [change[:description][:bullet]], type: :bullet %>
          <% end %>
        <% end %>
      </div>
    <% end %>
  </div>
<% else %>
  <p class="govuk-body">No changes have been made to this <%= @record.model_name.human.downcase %>.</p>
<% end %>
