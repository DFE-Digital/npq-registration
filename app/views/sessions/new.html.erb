<% if flash[:error].present? %>
  <%= govuk_notification_banner(title_text: "Failure") do |banner| %>
    <% banner.heading(text: flash[:error]) %>
  <% end %>
<% end %>

<%=
  render GovukComponent::StartButtonComponent.new(
    text: "Start TRA Identify Verification via openid connect",
    href: user_tra_openid_connect_omniauth_authorize_path,
    as_button: true,
    html_attributes: { method: :post }
  )
%>
