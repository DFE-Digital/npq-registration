<h1 class="govuk-heading-l">Bulk operations</h1>

<%=
  govuk_table do |table|
    table.with_body do |body|
      body.with_row do |row|
        row.with_cell(text: govuk_link_to("Revert applications to pending", npq_separation_admin_bulk_operations_revert_applications_to_pending_path))
      end
    end
  end
%>

