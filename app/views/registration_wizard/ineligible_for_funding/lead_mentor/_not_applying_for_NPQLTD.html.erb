<% content_for :title do %>
  <%= @form.errors.present? ? "Error: " : nil %> Funding
<% end %>

<% npqltd = Course.npqltd.first %>

<h1 class="govuk-heading-xl">Funding</h1>

<p class="govuk-body">
You’re not eligible for scholarship funding for <%= localise_sentence_embedded_course_name(course) %>
because you do not work in one of the eligible settings, such as state-funded schools.
<p class="govuk-body">This means that you would need to pay for the course another way.
</p>

<p class="govuk-body">
However, as a lead mentor for an accredited ITT provider, you’re eligible for scholarship funding for <%= localise_sentence_embedded_course_name(npqltd) %>.
</p>

<p class="govuk-body">You can <a class="govuk-link" href="<%= registration_wizard_show_url(@wizard.previous_step_path) %>"> go back and select <%= localise_sentence_embedded_course_name(npqltd) %></a> instead.</p>


