<% if tra_get_an_identity_omniauth_integration_active? %>
  <%=
    render GovukComponent::StartButtonComponent.new(
      text: "Start now",
      href: user_tra_openid_connect_omniauth_authorize_path,
      as_button: true,
      html_attributes: { method: :post }
    )
  %>
<% else %>
  <%= render GovukComponent::StartButtonComponent.new(
    text: "Start now",
    href: registration_wizard_show_path(@wizard.next_step_path)) %>
<% end %>
